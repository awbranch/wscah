import React from "react";
import { ImageCard as Props } from "@/types/ImageCard";

export default function ImageCard({
  image,
  title, 
  text,
  button,
  href
}: Props) {
  return <div>Image Card Goes Here</div>;
}
