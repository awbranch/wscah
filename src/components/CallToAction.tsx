import React from "react";
import { CallToAction as Props } from "@/types/CallToAction";

export default function CallToAction({
  blockId,
  blockPalette,
  orientation,
  image,
  eyebrow,
  title,
  text,
  button,
}: Props) {
  return <div>CALL TO ACTION GOES HERE</div>;
}
