import React from "react";
import { TitleCard as Props } from "@/types/TitleCa  rd";

export default function TitleCard({
  eyebrow,
  title, 
  text
}: Props) {
  return <div className="rounded-lg">
    Title Card Goes Here
    </div>;
}
