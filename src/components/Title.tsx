import React from "react";
import { Title as Props } from "@/types/Title";

export default function Title({
  blockId,
  blockPalette,
  eyebrow,
  title,
  text,
}: Props) {
  return <div>TITLE GOES HERE</div>;
}
